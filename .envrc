# direnv configuration for ios-media-toolkit
# Run `direnv allow` to activate

# Load .env file if it exists
dotenv_if_exists

# Add local bin to PATH for uv if installed via curl
path_add PATH "$HOME/.local/bin"
