# iOS Media Toolkit - Environment Configuration
# Copy this file to .env and adjust values as needed

# Required: Set your source and output paths
IMT_SOURCE_BASE=/path/to/iphone/albums
IMT_OUTPUT_BASE=/path/to/output
IMT_FAVORITES_OUTPUT=/path/to/output/favorites

# Optional: Custom config directory (default: ~/.config/ios-media-toolkit)
# IMT_CONFIG_DIR=/path/to/config

# Optional: Custom tool paths (if not in PATH)
# IMT_DOVI_TOOL=/path/to/dovi_tool
# IMT_MP4MUXER=/path/to/mp4muxer
